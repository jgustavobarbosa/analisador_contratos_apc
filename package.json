{
  "name": "analisador-contratos-planos",
  "private": true,
  "packageManager": "pnpm@11.24.0",
  "scripts": {
    "dev:api": "pnpm --filter api start:dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm --filter api test"
  },
  "engines": {
    "node": ">=20"
  }
}
