# Copy to apps/api/.env — see repo root .env.example

DATABASE_URL=postgresql://rayia:rayia@localhost:5432/rayia_identity?schema=public
API_PORT=3000
NODE_ENV=development
SESSION_COOKIE_NAME=rayia_session
SESSION_TTL_HOURS=12
SESSION_SECRET=change-me-to-a-long-random-secret
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
PASSWORD_RESET_TTL_MINUTES=30
APP_PUBLIC_URL=http://localhost:5180
LOGIN_MAX_FAILURES=5
LOGIN_LOCKOUT_MINUTES=15
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
RATE_LIMIT_AUTH_MAX=20
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=ChangeMeAdmin1!

# CORS para front no Vercel (vírgula)
# CORS_ORIGINS=https://seu-app.vercel.app
# COOKIE_SECURE=true
# COOKIE_SAMESITE=none

# OmniRoute LLM (local gateway — never commit real keys)
OMNIROUTE_BASE_URL=http://localhost:20128/v1
OMNIROUTE_API_KEY=
OMNIROUTE_MODEL=kc/deepseek/deepseek-v4-flash
OMNIROUTE_TIMEOUT_MS=60000
