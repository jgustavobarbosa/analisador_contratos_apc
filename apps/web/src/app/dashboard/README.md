# Dashboard de auditoria (Prompt 3)

Interface de demonstração visual B2B/Healthcare.

- **Path canônico no layout do produto:** `src/app/dashboard` (symlink → `apps/web/src/app/dashboard`)
- **Implementação / build:** `apps/web/src/app/dashboard` (Vite + Tailwind v4)
- Dados: `GET /demo/providers/:type/profile` (TRACK SIMULATION)

Toggle: **Base Simulada (Modo Demonstração)** vs **Base de Produção**.
